<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iflytek.om.dao.CollectionDao">

	<select id="addCollection" parameterType="map">
		insert into t_collection values( #{uid} ,#{mid})
	</select>



</mapper>